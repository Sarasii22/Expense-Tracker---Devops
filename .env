# Environment variables for docker-compose
# Keep secrets out of version control in real projects. This example uses a demonstration secret.
JWT_SECRET=supersecret_jwt_key_for_dev_only
PORT=5000
MONGO_INITDB_DATABASE=expense_tracker_db
